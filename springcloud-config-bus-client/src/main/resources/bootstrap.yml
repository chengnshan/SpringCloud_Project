
spring:
  application:
    name: springcloud-config-bus-client
  cloud:
    config:
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: springcloud-config-bus-server

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/,http://localhost:8763/eureka/




